if the Kafka send fails after commit, the event is lost — the production fix is a transactional outbox table drained to Kafka. Noted in code.
FCM push notifications are not sent to the device yet
ucha wuov qmus lfnt
